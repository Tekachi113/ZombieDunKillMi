# Tests

Unit and integration tests go here.

For now this directory is a placeholder — tests will be added as each system matures.

Suggested test framework: **doctest** (header-only, easy to add via vcpkg).
